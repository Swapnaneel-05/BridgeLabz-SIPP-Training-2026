While arrays in C can be fully stack-allocated, strings in high-level languages (Java, C#, Python) almost always store their character data on the heap due to their variable size and dynamic nature, with only the reference pointer residing on the stack.  C++ offers a hybrid approach via Small String Optimization (SSO), where short strings may be stored directly on the stack within the object.